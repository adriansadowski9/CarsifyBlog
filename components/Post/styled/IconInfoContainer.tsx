import styled from 'styled-components';

const IconInfoContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: flex-end;
  margin-right: ${(props) => props.theme.spaces.xxxl};
`;

export default IconInfoContainer;
